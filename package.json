{
  "name": "learn-sass",
  "version": "1.0.0",
  "description": "",
  "main": "gulpfile.js",
  "scripts": {
    "build": "gulp dist && gulp sass && node index.js",
    "bld": "gulp dist && gulp sass && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.4.1",
    "gulp": "^3.9.1",
    "gulp-sass": "^2.2.0",
    "highlight.js": "^9.3.0",
    "lodash": "^4.10.0",
    "marked": "^0.3.5"
  }
}
