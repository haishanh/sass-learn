{
  "name": "learn-sass",
  "version": "1.0.0",
  "description": "",
  "main": "gulpfile.js",
  "scripts": {
    "deploy": "deploy -d dist -b gh-pages",
    "build": "gulp dist && gulp sass && node render.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-sass": "^2.2.0",
    "highlight.js": "^9.3.0",
    "lodash": "^4.10.0",
    "marked": "^0.3.5",
    "pug": "^2.0.0-beta4"
  }
}
